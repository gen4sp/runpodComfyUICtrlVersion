
# Пример переменных окружения для runpod-comfyui-version-control
# Скопируйте этот файл в .env и настройте значения под свою конфигурацию

# === Базовые пути ===
# Базовая директория установки ComfyUI (по умолчанию внутри контейнера `/runpod-volume/ComfyUI`)
# Пример для локального запуска:
COMFY_HOME=$HOME/comfy

# Базовая директория моделей (по умолчанию $COMFY_HOME/models)
# Пример для локального запуска:
MODELS_DIR=$HOME/comfy/models

# === Управление версиями ===
# Удобное имя версии (используется для поиска lock-файла)
COMFY_VERSION_NAME=v0.3.59

# Явный путь к lock-файлу (перебивает COMFY_VERSION_NAME)
# LOCK_PATH=/path/to/custom.lock.json

# === Настройки вывода ===
# Режим вывода результата: gcs (по умолчанию) или base64
OUTPUT_MODE=gcs

# === Google Cloud Storage настройки ===
# Имя bucket для загрузки результатов
GCS_BUCKET=htv

# Путь к JSON ключу сервисного аккаунта (обязателен для GCS)
GOOGLE_APPLICATION_CREDENTIALS=/runpod-volume/creds/gc-service-account-key.json

# ID проекта GCP
GOOGLE_CLOUD_PROJECT=lia-profiler

# Префикс ключа в bucket (по умолчанию comfy/outputs)
GCS_PREFIX=comfy/outputs

# Количество попыток при загрузке (по умолчанию 3)
GCS_RETRIES=3

# Базовая задержка перед повтором в секундах (по умолчанию 0.5)
GCS_RETRY_BASE_SLEEP=0.5

# Если true, объекту ставится ACL public-read
GCS_PUBLIC=true

# Если >0, будет сгенерирован подписанный URL на TTL секунд
#GCS_SIGNED_URL_TTL=3600

# Если true (по умолчанию), выполняется проверка доступа к bucket при старте
GCS_VALIDATE=true

#download models
HF_TOKEN=
CIVITAI_TOKEN=

# === Таймауты ===
# Таймаут загрузки моделей в секундах (по умолчанию 180)
COMFY_MODELS_TIMEOUT=180

# Таймаут ожидания ответа от ComfyUI workflow в секундах (по умолчанию 300)
COMFY_RESPOND_TIMEOUT=300

//go